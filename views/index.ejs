<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
</head>
<body>
    <h1>Welcome, <%= user.name %></h1>
    <a href="/logout">Logout</a>
    <h2>Pizzas Available:</h2>
    <ul>
        <% pizzas.forEach(pizza => { %>
            <li>
                <h3><%= pizza.name %></h3>
                <p><%= pizza.description %></p>
                <p>Price: ₹<%= pizza.price %></p>
            </li>
        <% }) %>
    </ul>
</body>
</html>
